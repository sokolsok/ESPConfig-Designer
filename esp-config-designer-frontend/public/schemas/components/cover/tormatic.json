{
  "id": "cover.tormatic",
  "domain": "cover",
  "platform": "tormatic",
  "helpUrl": "https://esphome.io/components/cover/tormatic/",
  "extends": "base_cover.json",
  "requirements": [
    {
      "require": "bus:uart"
    }
  ],
  "fields": [
    {
      "key": "name",
      "placeholder": "Garage Door",
      "type": "text",
      "required": true,
      "lvl": "simple"
    },
    {
      "key": "open_duration",
      "type": "duration",
      "required": false,
      "lvl": "normal",
      "note": "Requires Tormatic UART setup (9600 baud, 8N1) and proper 5V logic level shifting on the UART side.",
      "default": "15s"
    },
    {
      "key": "close_duration",
      "type": "duration",
      "required": false,
      "lvl": "normal",
      "note": "Requires Tormatic UART setup (9600 baud, 8N1) and proper 5V logic level shifting on the UART side.",
      "default": "22s"
    }
  ]
}
