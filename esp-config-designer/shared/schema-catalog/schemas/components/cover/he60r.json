{
  "id": "cover.he60r",
  "domain": "cover",
  "platform": "he60r",
  "helpUrl": "https://esphome.io/components/cover/he60r/",
  "extends": "base_cover.json",
  "requirements": [
    {
      "require": "bus:uart"
    }
  ],
  "fields": [
    {
      "key": "name",
      "placeholder": "Cover Name",
      "type": "text",
      "required": false,
      "lvl": "simple"
    },
    {
      "key": "open_duration",
      "type": "duration",
      "required": false,
      "lvl": "normal",
      "note": "Requires HE60R UART bus setup (1200 baud, even parity, 1 stop bit) on the matching UART component.",
      "default": "15s"
    },
    {
      "key": "close_duration",
      "type": "duration",
      "required": false,
      "lvl": "normal",
      "note": "Requires HE60R UART bus setup (1200 baud, even parity, 1 stop bit) on the matching UART component.",
      "default": "15s"
    }
  ]
}
