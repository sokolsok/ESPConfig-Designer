{
  "id": "time.sntp",
  "domain": "time",
  "platform": "sntp",
  "helpUrl": "https://esphome.io/components/time/sntp/",
  "extends": "base_time.json",
  "requirements": [
    {
      "require": "network:network"
    }
  ],
  "fields": [
    {
      "key": "servers",
      "type": "list",
      "required": false,
      "lvl": "normal",
      "placeholder": "0.pool.ntp.org",
      "item": {
        "type": "text",
        "suppressQuotes": true,
        "placeholder": "0.pool.ntp.org"
      },
      "default": [
        "0.pool.ntp.org",
        "1.pool.ntp.org",
        "2.pool.ntp.org"
      ]
    }
  ]
}
