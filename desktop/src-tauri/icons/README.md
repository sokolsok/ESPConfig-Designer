`icon.ico` is a development build input generated from the existing
ESPConfig Designer artwork. Final release branding and signed packaging are
still deferred.

The development shell uses this icon for the unsigned NSIS development/test
package. Neither the installer nor the installed executable is a release
artifact.
