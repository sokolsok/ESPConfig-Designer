{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal desktop shell permissions.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
